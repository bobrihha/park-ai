# 🚧 Jungle City Bot — Контекст проекта

> **ВАЖНО**: Этот файл предназначен для AI-агентов. Читай внимательно перед любыми изменениями!

---

## 📍 Расположение версий

| Версия | Путь | Статус | Порт |
|--------|------|--------|------|
| **Тестовая** | `/Users/nataliabobrovskaya/dev/parki/park 1/test park` | 🔧 В разработке | localhost:8000 |
| **Боевая** (сервер) | `/root/jungle_bot` (95.81.99.32) | ✅ Работает | 8000 |

---

## 🎯 Цель

Тестовая версия должна стать **новой боевой** — чистой, понятной, полной.  
Текущая боевая — "бардак", но содержит полезные фичи.

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

1. **НЕ ТРОГАЙ боевую версию** без явного разрешения
2. **НЕ СМЕШИВАЙ токены** — у тестовой свои боты/каналы
3. **НЕ ПОДКЛЮЧАЙ тестовую к AmoCRM** — операторы работают
4. Все изменения — только в тестовой версии

---

## ✅ Реализовано в тестовой

- [x] Чистая архитектура (Flows, Adapters, Services)
- [x] Кнопки выбора формата/времени (TG, VK, Web)
- [x] Lead Service с CRM-подготовкой
- [x] Админка с промптами
- [x] RAG (база знаний)

## ❌ НЕ реализовано в тестовой (нужно перенести из боевой)

- [ ] **AmoCRM lookup** — при первом контакте искать контакт по TG/VK ID
- [ ] **Pending phone confirm** — подтверждение телефона из CRM после ввода количества детей
- [ ] **Регистрация в веб-чате** — форма имя/телефон перед началом чата
- [ ] **Цветовая схема** — фиолетовый `#43348b` вместо зелёного
- [ ] **Аватар** — `chat-avatar.jpg` вместо эмодзи 🐒
- [ ] **Обновление сделки** — добавлять доп. услуги в существующую сделку

---

## 🔧 Технические детали

### AmoCRM Lookup (боевая реализация)
```python
# При нажатии "🎂 Организовать праздник":
contact = await amocrm_client.find_contact_by_telegram_id(user.id)
if contact:
    contact_info = amocrm_client.get_contact_info(contact)
    found_phone = contact_info.get("phone")
    found_name = contact_info.get("name")
    
    # Сохраняем для подтверждения ПОСЛЕ количества детей
    context.user_data["pending_phone_confirm"] = found_phone
    context.user_data["pending_customer_name"] = found_name
```

### Цвета виджета (боевая)
```javascript
primaryColor: '#43348b',  // Фиолетовый (сейчас #4CAF50 зелёный)
botAvatarUrl: 'https://junglbot-nn.cachalot.cc/static/chat-avatar.jpg'
```

### Форма регистрации (боевая)
```html
<!-- В веб-чате ПЕРЕД началом диалога -->
<div id="jc-register-form">
  <input id="jc-reg-name" placeholder="Ваше имя">
  <input id="jc-reg-phone" placeholder="+7 (___) ___-__-__">
  <button>Начать чат</button>
</div>
```

---

## 📁 Ключевые файлы

| Файл | Назначение |
|------|------------|
| `core/amocrm.py` | Интеграция с AmoCRM |
| `core/flows/birthday.py` | Логика флоу ДР |
| `bot/handlers.py` | Telegram handlers |
| `bot/vk_bot.py` | VK bot |
| `api/main.py` | Web chat API |
| `api/widget.html` | Веб-виджет |
| `config/prompts.py` | Промпты AI |
| `admin/app.py` | Админка Streamlit |

---

## 🔐 Доступы

### Сервер (боевая)
```
IP: 95.81.99.32
User: root
Pass: Narzan@0
```

### Тестовые каналы
- Telegram бот: тестовый токен в .env
- VK группа: тестовая
- Каналы уведомлений: тестовые

---

## 📋 План миграции

1. ✅ Изучить боевую версию
2. ⏳ Перенести недостающие фичи в тестовую
3. ⏳ Развернуть тестовую на сервер в отдельную папку
4. ⏳ Тестирование с тестовыми ботами
5. ⏳ Когда готово — переключить токены, удалить старую боевую
